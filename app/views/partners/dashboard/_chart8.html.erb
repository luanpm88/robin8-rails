<div class="card mb-12">
  <div class="card-header">

  </div>
  <div class="card-body">
    <div class="row">
      <div class="col-sm-12 my-auto">
        <canvas id="chart8" width="100" height="100"></canvas>
      </div>
    </div>
  </div>
  <div class="card-footer small text-muted"><i class="fa fa-bar-chart"></i>用户标签统计</div>
</div>

<script type="text/javascript">
  $(document).ready(function () {
    var chartJson = $.ajax({url: "/partners/dashboard/chart8.json", async: false});
    console.log(chartJson.responseText);
    //  alert(chartJson.responseText);

    var array = JSON.parse(chartJson.responseText);
    //  alert("array " +array);

    var chartlabels = array.labels;
    //   alert("xxx" + chartlabels);
    console.log(chartlabels);
    var chartData = array.data;
    var ctx = document.getElementById("chart8");
    // alert("ctx = " +ctx);
    var myLineChart = new Chart(ctx, {
      //   type: 'bar',
      type: 'horizontalBar',
      data: {
        labels: chartlabels, //["January", "February", "March", "April", "May"],
        datasets: [{
          label: "用户标签统计",
          //   backgroundColor: "rgba(2,117,216,1)",
          backgroundColor: [
          //  #007bff","#dc3545","#ffc107","#28a745"
            'rgba(0, 93, 255, 0.8)',
            'rgba(208, 53, 69, 0.8)',
            'rgba(255, 193, 7, 0.8)',
            'rgba(40, 107, 69, 0.8)',
            'rgba(0, 159, 255, 0.8)'
          ],
          borderColor: "rgba(2,117,216,1)",
          data: chartData, //[4215, 5312, 6251, 7841, 9821],
        }],
      },
      options: {
        scales: {
          xAxes: [{

            gridLines: {
              display: true
            },
            ticks: {
             // max: 100,
              maxTicksLimit: 6

            }
          }],
          yAxes: [{
            ticks: {
              min: 0,
              max: 15000,
              maxTicksLimit: 5
            },
            gridLines: {
              display: true
            }
          }],
        },
        legend: {
          display: false
        }
      }
    });
  });
</script>
