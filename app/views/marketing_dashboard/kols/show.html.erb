<table class="table table-striped">
  <% @kol.attributes.except(*sensitivity_list).each do |k, v| %>
    <tr>
      <td> <%= k.classify %></td>
      <td> <%= v %> </td>
    </tr>

  <% end %>
</table>
