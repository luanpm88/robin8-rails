<div class="container-fluid">
  <h2>
    创建夺宝活动
    <%= link_to "返回上一页", marketing_dashboard_lottery_activities_path, class: "btn btn-default pull-right" %>
  </h2>
  <hr/>

  <%= form_for([:marketing_dashboard, @lottery_activity], url: marketing_dashboard_lottery_activities_path, html: { multiple: true}) do |f| %>
    <%= render partial: 'form', locals: {f: f} %>
  <% end %>
</div>
