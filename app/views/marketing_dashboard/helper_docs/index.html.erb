<div>
  <h3>问题列表</h3>
  <%= link_to "新建问题", new_marketing_dashboard_helper_doc_path, :class => "btn pull-right"%>
</div>
<table class='table table-bordered'>
  <thead>
    <tr>
      <th>编号</th>
      <th>分类名</th>
      <th>问题</th>
      <th>答案</th>
      <th>权重排序</th>
      <th>操作</th>
    </tr>
  </thead>

  <tbody>
    <% @helper_docs.each do |h| %>
      <tr>
        <td><%= h.id %></td>
        <td><%= h.doc_tag %></td>
        <td><%= h.question %></td>
        <td><%= h.answer %></td>
        <td><%= h.sort_weight %></td>
        <td>
          <%= link_to "编辑", edit_marketing_dashboard_helper_doc_path(h) %> | 
          <%= link_to "删除", marketing_dashboard_helper_doc_path(h), :method => :delete, "data-confirm" => "你确定要删除吗"%>
        </td>
      </tr>
    <% end %>
  </tbody>

</table>

<%= will_paginate @helper_tags %>
