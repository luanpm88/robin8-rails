<%= form_tag marketing_dashboard_campaign_invite_reject_path do %>
  <h3>请输入拒绝理由（非必填）</h3>
  <div class="col-sm-2">
    <div class="form-group"><input type="text" name='reject_reason' class='form-control'></div>

  </div>

  <%= submit_tag '拒绝', :class => 'btn btn-primary' %>

<% end %>
