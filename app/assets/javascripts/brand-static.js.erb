<%
Dir.new(File.join(Rails.root, 'app', 'assets', 'webpack')).select{ |x| x =~ /brand-vendor-bundle-(.*).js$/ || x =~ /brand-app-bundle-(.*).js$/ }.sort.reverse.each do |file|
  require_asset(file)
end
%>