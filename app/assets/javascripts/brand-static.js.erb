<%
Dir.new(File.join(Rails.root, 'app', 'assets', 'webpack')).select{ |x| x =~ /brand-vendor-bundle_(.*).js$/ || x =~ /brand-app-bundle_(.*).js$/ }.sort.reverse.each do |file|
  require_asset(file)
end
%>